services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: milesight-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto:/mosquitto/config:ro
      - mosquitto_data:/mosquitto/data

volumes:
  mosquitto_data:
